@model UserListViewModel
@{
    ViewBag.Title = "Title";
    Layout = "_AdminLayout";
}

<div class="row">
    <div class="col-12">
        <a asp-controller="User" asp-action="Add" class="btn btn-primary">Yeni Kullanıcı Ekle</a>
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Kullanıcı Listesi</h3>
            </div>
            <!-- /.card-header -->
            <div class="card-body">
                <table id="example2" class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>Ad</th>
                            <th>Soyad</th>
                            <th>E-Mail</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model.Users)
                        {
                            <tr>
                                <td>@user.UserName</td>
                                <td>@user.UserSurname</td>
                                <td>@user.Email</td>
                                <td>

                                    <a asp-controller="User" asp-action="Update" asp-route-id="@user.UserId" class="btn btn-warning btn-sm">Güncelle</a>

                                    <a asp-controller="User" asp-action="Delete" asp-route-id="@user.UserId" class="btn btn-danger btn-sm">Sil</a>

                                </td>



                            </tr>
                        }

                    </tbody>

                </table>
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->
    </div>
    <!-- /.col -->
</div>
<!-- /.row -->
<!-- Page specific script -->
@section TabloScript{
    <script>
        $(function () {
            $('#example2').DataTable({
                "paging": true,
                "lengthChange": false,
                "searching": false,
                "ordering": true,
                "info": true,
                "autoWidth": false,
                "responsive": true,
            });
        });
    </script>
}